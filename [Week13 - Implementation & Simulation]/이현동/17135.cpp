#include <bits/stdc++.h>
using namespace std;
using pii = std::pair<int, int>;


int N, M, D;
int MAP[16][16];

void bfs()
{
    queue<pii>
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> N >> M >> D;
    for (int i = 0; i < N;i++){
        for (int j = 0; j < M;j++){
            cin >> MAP[i][j];
        }
    }

    return 0;
}